2.28日修正：

由于tfrecords方式读入数据是按样本提前对源数据进行处理并序列化，需要预处理时间较长。此处改为以h5格式直接以numpy.ndarray格式读入数据并训练，预处理速度大大提升。

h5_generate: 按照预先生成.npy文件读入数据生成h5数据集，并为模型读入训练数据提供接口。

------------------------------------------

该方案是利用LeNet5模型对轴承故障信号转化而成的灰度图进行识别分类。

在LeNet5模型训练之前，需要对所用一位振动信号进行灰度图转换并存储，建立对应的灰度图数据集。

各子程序功能如下

get_greyimages.py: 将保存为mat格式的原始一维数据读入并转为灰度图存储，用于后续建立模型训练所需数据集。

label_generate.py: 给所有灰度图打上对应类别标签，对应信息存储于label.txt文件中

train_test_split.py: 对数据集按指定比例随机抽取划分为训练集与测试集。

tfrecord_generate.py: 模型基于tensorflow搭建，此处选择对建立好的数据集利用tfrecord数据接口进行序列化，方便后续数据的读入。

grey_lenet5_net.py: LeNet5模型，在此进行模型的训练及结果输出。

predict.py: 该程序用于读入已训练好的模型来对给定数据进行预测。

tsne.py: 用于输出LeNet5模型的t-SNE图，来判断模型的特征区分能力。

程序运行顺序： get_greyimages.py --> label_generate.py --> train_test_split.py --> tfrecord_generate.py --> grey_lenet5_net.py --> (predict.py | tsne.py)
