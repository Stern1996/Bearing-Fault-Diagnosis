该方案是利用CNN模型对轴承故障信号通过时频转换后得到的特征图进行识别，从而对轴承故障类型进行分类。

在CNN模型训练之前，需要对所用数据集进行处理。模型所识别数据为基于振动信号的时频转换图，故会先对数据进行转换、整理。

各子程序功能如下

STFT_Images.py: 将保存为mat格式的原始一维数据读入并进行时频转换后分类存储，用于后续建立模型训练所需数据集。

label_generate.py: 给转换后的时频图打上对应类别标签，对应信息存储于label.txt文件中

train_test_split.py: 对数据集按指定比例随机抽取划分为训练集与测试集。

tfrecord_generate.py: 模型基于tensorflow搭建，此处选择对建立好的数据集利用tfrecord数据接口进行序列化，方便后续数据的读入。

stft_CNN_net.py: CNN模型，在此进行模型的训练及结果输出。

predict.py: 该程序用于读入已训练好的模型来对给定数据进行预测。

tsne.py: 用于输出CNN模型的t-SNE图，来判断模型的特征区分能力。

程序运行顺序： STFT_Images.py --> label_generate.py --> train_test_split.py --> tfrecord_generate.py --> stft_CNN_net.py --> (predict.py | tsne.py)
