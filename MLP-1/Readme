该方案是利用含1层隐藏层的多层感知机对轴承故障信号直接进行识别分类。

在模型训练之前，需要对所用数据集进行预处理。

各子程序功能如下

get_segments.py: 将保存为mat格式的原始一维数据读入存储，用于后续建立模型训练所需数据集。（从4种故障类别中分别随机抽取5200个样本，每个样本包含2048个数据点）

label_generate.py: 给所有样本打上对应类别标签，对应信息存储于label.txt文件中

train_test_split.py: 对数据集按指定比例随机抽取划分为训练集与测试集。

tfrecord_generate.py: 模型基于tensorflow搭建，此处选择对建立好的数据集利用tfrecord数据接口进行序列化，方便后续数据的读入。

MLP-1_net.py: MLP模型，在此进行模型的训练及结果输出。

predict.py: 该程序用于读入已训练好的模型来对给定数据进行预测。

tsne.py: 用于输出MLP模型的t-SNE图，来判断模型的特征区分能力。

程序运行顺序： get_segments.py --> label_generate.py --> train_test_split.py --> tfrecord_generate.py --> MLP-1_net.py --> (predict.py | tsne.py)
